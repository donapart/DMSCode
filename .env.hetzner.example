# Copy to .env.hetzner and adjust
# Domain pointing to your Hetzner VPS (A record)
DMS_SERVER_NAME=api.example.com

# Optional: protect all endpoints with X-API-KEY header
# If empty, nginx will not enforce an API key.
DMS_API_KEY=

# LLM Provider: ollama, anthropic, openai
LLM_PROVIDER=anthropic

# Anthropic API Key (required if LLM_PROVIDER=anthropic)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-haiku-20240307

# OpenAI API Key (required if LLM_PROVIDER=openai)
OPENAI_API_KEY=

# Ollama model (used if LLM_PROVIDER=ollama)
OLLAMA_MODEL=llama3.2:3b

# Optional: override embedding model (defaults to multilingual MiniLM)
EMBEDDING_MODEL=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2

# Optional: default voice for Piper
DEFAULT_VOICE=de_DE-thorsten-low
